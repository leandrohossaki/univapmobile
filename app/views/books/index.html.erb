<h1>Lista de Livros</h1>

<table class="grid">  
  <tr>
    <th colspan="3" align="left"><%= link_to image_tag('add.png', :border => 0), new_book_path, :title => 'Adicionar' %></th>
    <th>Código</th>
    <th>Categoria</th>
    <th>Nome</th>
    <th>Autor</th>
    <th>Editora</th>
    <th>Unidade</th>
  </tr>

<% @books.each do |book| %>
  <tr>
    <td><%= link_to image_tag('view.png', :border => 0), book, :title => 'Visualizar' %></td>
    <td><%= link_to image_tag('edit.png', :border => 0), edit_book_path(book), :title => 'Editar' %></td>
    <td><%= link_to image_tag('delete.png', :border => 0), book, confirm: 'Deseja realmente excluir o registro?', method: :delete, :title => 'Excluir' %></td>
    <td><%= book.id %></td>
    <td><%= book.category_id %></td>
    <td><%= book.name %></td>
    <td><%= book.author %></td>
    <td><%= book.editor %></td>
    <td><%= book.unit_id %></td>
  </tr>
<% end %>
</table>

<br />
